# Uncomment the next line to define a global platform for your project
platform :osx, '10.14'

target 'ImageTemplateFinder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageTemplateFinder
  pod 'SwiftLint'

  target 'ImageTemplateFinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageTemplateFinderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
